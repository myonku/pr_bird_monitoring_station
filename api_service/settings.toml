[runtime]
entity_type = "service"
entity_id = "api_service"
entity_name = "api_service"

[auth]
secret_key_dir = "secret_keys"
active_key_id = "api-local-key"

[forwarded_auth]
enabled = false
# 去旧兼容：不再支持 reject-stub，启用前必须注入 authority client 并装配 authority-backed verifier。

[redis]
mode = "single"
dialect = "redis"
# host = "127.0.0.1"
# port = 6379
database = 0
# password = ""
